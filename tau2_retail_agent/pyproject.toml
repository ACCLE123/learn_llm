[project]
name = "tau2-retail-agent"
version = "0.1.0"
description = "A local Qwen tool agent for τ² Retail experiments."
requires-python = ">=3.12,<3.14"

[tool.unittest]
start-directory = "tests"

[tool.ruff]
line-length = 100
target-version = "py312"
